{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-semantic-protocols.local/schemas/semantic-source-location.v1.schema.json",
  "title": "Semantic Source Location",
  "description": "Shared source path, structural selector, line range, and source locator base used by semantic query, search, read, tree-sitter provenance, and native syntax fact schemas. This schema is not a packet envelope. Structural selectors are stable element identities; line ranges are display and compatibility hints.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "schemaId": {
      "const": "agent.semantic-protocols.semantic-source-location"
    },
    "schemaVersion": {
      "const": "1"
    },
    "path": {
      "$ref": "#/$defs/projectPath"
    },
    "lineRange": {
      "$ref": "#/$defs/lineRange"
    },
    "structuralSelector": {
      "$ref": "#/$defs/structuralSelector"
    },
    "displayLineRange": {
      "$ref": "#/$defs/lineRange"
    },
    "sourceLocatorHint": {
      "$ref": "#/$defs/sourceLocator"
    },
    "projection": {
      "$ref": "#/$defs/projection"
    },
    "codePolicy": {
      "$ref": "#/$defs/codePolicy"
    },
    "location": {
      "$ref": "#/$defs/location"
    },
    "sourceLocator": {
      "$ref": "#/$defs/sourceLocator"
    },
    "sourceSpanLocator": {
      "$ref": "#/$defs/sourceSpanLocator"
    }
  },
  "$defs": {
    "projectPath": {
      "type": "string",
      "description": "Canonical project-root-relative path. This is a path value, not a display locator: no rank prefixes, URI schemes, command prefixes, line ranges, columns, absolute paths, parent segments, backslashes, or numeric node slots.",
      "minLength": 1,
      "pattern": "^(?:\\.|(?!/)(?![A-Za-z]:)(?![0-9]+:)(?!\\.\\.?($|/))(?!.*(^|/)\\.\\.?($|/))(?!.*//)[^\\s:\\\\]+(?:/[^\\s:\\\\]+)*)$"
    },
    "lineRange": {
      "type": "string",
      "description": "Compact source line range as start:end, for example 10:43. This is a display or compatibility hint, not a semantic selector identity.",
      "pattern": "^[1-9][0-9]*:[1-9][0-9]*$"
    },
    "structuralSelector": {
      "type": "string",
      "description": "Parser-owned structural selector for an AST/document element. It identifies the element by language, project path, and structural path rather than by line range. The rightmost # separates the verbatim project-relative owner path from the canonical identity fragment, so # remains legal inside an owner filename.",
      "minLength": 1,
      "pattern": "^[a-z][a-z0-9+.-]*://[^\\s]+#[^\\s]+$"
    },
    "location": {
      "$ref": "semantic-definitions.v1.schema.json#/$defs/location"
    },
    "sourceLocator": {
      "$ref": "semantic-definitions.v1.schema.json#/$defs/sourceLocator"
    },
    "sourceSpanLocator": {
      "type": "string",
      "description": "Project-root-relative source selector requiring an explicit start:end span.",
      "pattern": "^(?:\\.|(?!/)(?![A-Za-z]:)(?![0-9]+:)(?!\\.\\.?($|/))(?!.*(^|/)\\.\\.?($|/))(?!.*//)[^\\s:\\\\]+(?:/[^\\s:\\\\]+)*):[1-9][0-9]*:[1-9][0-9]*$"
    },
    "projection": {
      "enum": [
        "skeleton",
        "outline",
        "names",
        "signature",
        "doc",
        "children",
        "content",
        "source",
        "callable-skeleton",
        "code"
      ],
      "description": "Requested or rendered projection for a semantic locator. Exact query uses source or callable-skeleton; content is the document projection."
    },
    "codePolicy": {
      "enum": [
        "no-code",
        "code-after-exact-selector",
        "requires-exact-code"
      ],
      "description": "Boundary for when a source locator hint may be materialized through an exact selector with projection=source."
    }
  }
}
