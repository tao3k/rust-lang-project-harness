harness = "libtest"
test = "scenario_benchmark_rust_package_edition_2024_v1_snapshot"
snapshot = "scenario_benchmark_rust_package_edition_2024_v1"
target_total = "5ms"
max_total = "25ms"
observed_total = "3ms"
regression_budget = "5ms"
memory_budget_bytes = 2097152
observed_memory_bytes = 1048576
target_rationale = "Manifest edition scenarios are metadata-only and should stay in the low millisecond fixture budget."

[input_expected_comparison]
input_total = "4ms"
expected_total = "3ms"
input_memory_bytes = 1048576
expected_memory_bytes = 1048576
interpretation = "Expected removes the stale edition finding with no extra runtime or memory cost; equal memory is acceptable because the fixture only changes Cargo metadata."
expected_not_faster_annotation = "Memory is unchanged because the expected fixture only updates Cargo edition metadata; the quality gain is compatibility with current Rust syntax rather than allocator reduction."

[observed_timings]
manifest_deserialize = "1ms"
policy_projection = "1ms"
snapshot_assertion = "1ms"
