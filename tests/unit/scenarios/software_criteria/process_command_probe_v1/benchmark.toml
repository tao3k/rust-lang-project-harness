harness = "libtest"
test = "scenario_benchmark_process_command_probe_v1_snapshot"
snapshot = "scenario_benchmark_process_command_probe_v1"
target_total = "25ms"
max_total = "100ms"
observed_total = "19ms"
regression_budget = "20ms"
memory_budget_bytes = 8388608
observed_memory_bytes = 4194304
target_rationale = "Parser-native process probe scenario checks must stay inside the small fixture budget."

[input_expected_comparison]
input_total = "31ms"
expected_total = "19ms"
input_memory_bytes = 6291456
expected_memory_bytes = 4194304
interpretation = "Expected keeps command construction, PATH shaping, output parsing, and receipts split while staying faster in this fixture; future scenarios may record input as faster when that is the real evidence."

[observed_timings]
contract_deserialize = "9ms"
receipt_render = "3ms"
snapshot_assertion = "2ms"
