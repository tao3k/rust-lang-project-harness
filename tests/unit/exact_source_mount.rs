use crate::exact_source::{
    PinnedWorkspace, RelocationOutcome, collect_parse_artifact_items,
    exact_source_merkle_root_depth, owner_live_item_diagnostics, relocate_live_item,
    resolve_live_item,
};

#[path = "cli/runner/exact_source.rs"]
mod tests;
